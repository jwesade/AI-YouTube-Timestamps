# Copy to .env and fill in. Never commit .env.

# Supabase (needed once we start writing to the DB)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic (needed once we add the normalizer agent)
ANTHROPIC_API_KEY=

# Google (needed once we add the Google Places source)
GOOGLE_PLACES_API_KEY=
